#include <iostream>
#include <chrono>
#include <thread>
#include <resources.h>

void moveSteps(bool dir, int steps){
    std::cout << dirPin;
    std::cout << stepPin;
    std::cout << dir;
    std::cout << steps;
}

bool homeCheck(){
    // read homePin
}
